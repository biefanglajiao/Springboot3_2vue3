server:
  port: 8080
logging:
  level:
    com.example.springboot3_2vue3.mapper: debug
spring:
  datasource:
    url: jdbc:mysql://120.78.134.171:3306/springboot3vue3?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false
    username: springboot3vue3
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver


    hikari:
      max-lifetime: 120000
  redis:
    # Redis本地服务器地址，注意要开启redis服务，即那个redis-server.exe
    host: 127.0.0.1
    # Redis服务器端口,默认为6379.若有改动按改动后的来
    port: 6379
    #Redis服务器连接密码，默认为空，若有设置按设置的来
    password:
    #如果需要在一个xml文件中执行多个sql语句 需要在url后面添加：&allowMultiQueries=true
    #  datasource:
    #    driver-class-name: com.mysql.cj.jdbc.Driver
    #    url: jdbc:mysql://localhost:3306/springboot32vue3?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8&allowMultiQueries=true
    #    username: root
    #    password: 020116zh
    #如果需要在一个xml文件中执行多个sql语句 需要在url后面添加：&allowMultiQueries=true
#mybatisplus
#以下为连接池的相关参数配置


#rocketmq配置
rocketmq:
  name-server: 127.0.0.1:9876
  producer:
    group: default
